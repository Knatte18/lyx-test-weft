batch: 01-write-alpha
status: stuck
tests: red
stuck_reason: "verify: `git cat-file -e 4b825dc642cb6eb9a060e54bf8d69288fbee4904:unfixable-verify-sentinel` always fails — that hash is git's fixed empty-tree object, which can never contain any path, so no code or commit change can satisfy it; ran the command twice (both self-fix attempts) with identical fatal 'path does not exist' result before stopping."
