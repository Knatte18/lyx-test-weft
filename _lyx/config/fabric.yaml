branch_prefix: ${env:LYX_BRANCH_PREFIX:-} # prefix prepended to the slug to form the branch name (e.g. "hanf/"); empty = branch == slug
pathspec: "" # OPTIONAL per-repo directory path(s) relative to worktree root, whitespace-separated; _lyx and .lyx are structural and injected in code by internal/fabricengine, never read from here
