discussion: sonnet[effort=low] # crucible r2 dummy task: cheap model per the campaign cost declaration
discussion_timeout_min: 30
discussion_interactive: false
plan: sonnet[effort=low]
plan_timeout_min: 20
review: sonnet[effort=low]
review_timeout_min: 30
selfreport: false # crucible r2: live-fire already captured (issues #240/#241); never re-trigger
friction: haiku # Tier 2 on with the cheapest model, selfreport stays off; reflection watched live
friction_timeout_min: 10
