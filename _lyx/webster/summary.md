# fable-r3 early-crash probe

The plan consisted of a single card, `01-lazarus`, which created `r3d.md` containing the
line `BACK` and committed it (head SHA `ba87013259f08c06f095e1f9d1edb9a07f406e39`). The
batch's implementer fork reported `status: OK` with no deviations from its declared
file-ops. The plan carries no plan-level `## verify:` section, so no integration stage
was run. All 1 batch in the plan completed successfully.
