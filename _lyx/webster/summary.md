# fable-r3 early-crash probe two

Single-card plan: batch 01-lazarus2 created `r3e.md` containing the line `BACK-2` and committed it (head_sha `de29c4c2f82cbdf55303972d584cd0d6b8aa3717`). The batch reported `status: done` with no deviations from its declared file-ops. The plan has no `## verify:` section, so no integration stage ran. All batches complete; run outcome is `done`.
