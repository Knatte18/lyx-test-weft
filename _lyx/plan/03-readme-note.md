# Card 3 — readme-note

**Prosa:**
- `//README.md`

**Intent:** The repository README describes the `services/api/` subpath fixture but predates the
farewell path. Add one short sentence to the existing `services/api/` description noting that the
package now exposes a farewell composer alongside the greeting one.

Prose only — one sentence, in the existing section, no new headings and no other file touched.

**Commit:** `3: readme-note`
